#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../esbuild/bin/esbuild" "$@"
else
  exec node  "$basedir/../../../esbuild/bin/esbuild" "$@"
fi
# cmd-shim-target=C:/Users/nekostul/Desktop/SoundCloud-DesktopFork-main/landing/node_modules/.pnpm/vite@7.3.1_jiti@2.6.1_lightningcss@1.31.1/node_modules/esbuild/bin/esbuild
